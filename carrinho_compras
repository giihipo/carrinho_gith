<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Loja Online - Carrinho Funcional</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* Custom styles for transitions */
        .cart-slide {
            transition: transform 0.3s ease-in-out;
        }
        .cart-open {
            transform: translateX(0);
        }
        .cart-closed {
            transform: translateX(100%);
        }
        .fade-in {
            animation: fadeIn 0.3s;
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        /* Custom scrollbar for cart */
        .cart-items::-webkit-scrollbar {
            width: 6px;
        }
        .cart-items::-webkit-scrollbar-track {
            background: #f1f1f1;
        }
        .cart-items::-webkit-scrollbar-thumb {
            background: #888;
            border-radius: 3px;
        }
        .cart-items::-webkit-scrollbar-thumb:hover {
            background: #555;
        }
    </style>
</head>
<body class="bg-gray-50 font-sans">
    <!-- Header/Navbar -->
    <header class="bg-white shadow-md sticky top-0 z-10">
        <div class="container mx-auto px-4 py-4 flex justify-between items-center">
            <div class="flex items-center">
                <i class="fas fa-shopping-bag text-2xl text-purple-600 mr-2"></i>
                <h1 class="text-xl font-bold text-gray-800">LojaOnline</h1>
            </div>
            <nav class="hidden md:flex space-x-8">
                <a href="#" class="text-gray-800 hover:text-purple-600 font-medium">Home</a>
                <a href="#" class="text-gray-800 hover:text-purple-600 font-medium">Produtos</a>
                <a href="#" class="text-gray-800 hover:text-purple-600 font-medium">Ofertas</a>
                <a href="#" class="text-gray-800 hover:text-purple-600 font-medium">Contato</a>
            </nav>
            <div class="flex items-center space-x-4">
                <button class="md:hidden text-gray-800">
                    <i class="fas fa-bars text-xl"></i>
                </button>
                <button id="cart-toggle" class="relative">
                    <i class="fas fa-shopping-cart text-xl text-gray-800 hover:text-purple-600"></i>
                    <span id="cart-count" class="absolute -top-2 -right-2 bg-purple-600 text-white text-xs font-bold rounded-full h-5 w-5 flex items-center justify-center">0</span>
                </button>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="container mx-auto px-4 py-8">
        <!-- Hero Section -->
        <section class="bg-gradient-to-r from-purple-500 to-indigo-600 rounded-xl p-8 text-white mb-12">
            <div class="max-w-xl">
                <h2 class="text-3xl md:text-4xl font-bold mb-4">As melhores ofertas estão aqui!</h2>
                <p class="text-lg mb-6">Descubra produtos incríveis com preços imperdíveis.</p>
                <button class="bg-white text-purple-600 px-6 py-2 rounded-lg font-medium hover:bg-gray-100 transition duration-300">Ver Ofertas</button>
            </div>
        </section>

        <!-- Product Grid -->
        <h2 class="text-2xl font-bold text-gray-800 mb-6">Nossos Produtos</h2>
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
            <!-- Product 1 -->
            <div class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-xl transition duration-300">
                <div class="relative">
                    <img src="amor.webp">
                    <span class="absolute top-2 right-2 bg-red-500 text-white text-xs font-bold px-2 py-1 rounded-full">OFERTA</span>
                </div>
                <div class="p-4">
                    <h3 class="font-bold text-lg mb-2">Morango Do Amor</h3>
                    <p class="text-gray-600 text-sm mb-3">Morango Do Amor</p>
                    <div class="flex justify-between items-center">
                        <div>
                            <span class="font-bold text-lg text-purple-600">R$ 299,90</span>
                        </div>
                        <button class="add-to-cart bg-purple-600 hover:bg-purple-700 text-white px-3 py-1 rounded-full text-sm transition duration-300" 
                                data-id="1" 
                                data-name="Morango Do Amor" 
                                data-price="24.90" 
                                data-image="https://images.unsplash.com/photo-1542291026-7eec264c27ff?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=600&q=80">
                            <i class="fas fa-cart-plus"></i> Adicionar
                        </button>
                    </div>
                </div>
            </div>

            <!-- Product 2 -->
            <div class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-xl transition duration-300">
                <div class="relative">
                    <img src="cu.jpg">
                    <span class="absolute top-2 right-2 bg-green-500 text-white text-xs font-bold px-2 py-1 rounded-full">NOVO</span>
                </div>
                <div class="p-4">
                    <h3 class="font-bold text-lg mb-2">Cookie Choco</h3>
                    <p class="text-gray-600 text-sm mb-3">Cookie Choco</p>
                    <div class="flex justify-between items-center">
                        <div>
                            <span class="font-bold text-lg text-purple-600">R$ 8,99</span>
                           
                        </div>
                        <button class="add-to-cart bg-purple-600 hover:bg-purple-700 text-white px-3 py-1 rounded-full text-sm transition duration-300" 
                                data-id="2" 
                                data-name="S,martwatch X200" 
                                data-price="499.90" 
                                data-image="https://images.unsplash.com/photo-1523275335684-37898b6baf30?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=600&q=80">
                            <i class="fas fa-cart-plus"></i> Adicionar
                        </button>
                    </div>
                </div>
            </div>

            <!-- Product 3 -->
            <div class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-xl transition duration-300">
                <img src="belem.jpg" alt="Fones de ouvido" class="w-full h-48 object-cover">
                <div class="p-4">
                    <h3 class="font-bold text-lg mb-2">Pastel De Belem</h3>
                   
                    <div class="flex justify-between items-center">
                        <div>
                            <span class="font-bold text-lg text-purple-600">10,99</span>
                        </div>
                        <button class="add-to-cart bg-purple-600 hover:bg-purple-700 text-white px-3 py-1 rounded-full text-sm transition duration-300" 
                                data-id="3" 
                                data-name="Pastel De Belem" 
                                data-price="10,99" 
                                data-image="https://images.unsplash.com/photo-1505740420928-5e560c06d30e?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=600&q=80">
                            <i class="fas fa-cart-plus"></i> Adicionar
                        </button>
                    </div>
                </div>
            </div>

            <!-- Product 4 -->
            <div class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-xl transition duration-300">
                <img src="du.jpg">
                <div class="p-4">
                    <h3 class="font-bold text-lg mb-2">donuts Americanos</h3>
                   
                    <div class="flex justify-between items-center">
                        <div>
                            <span class="font-bold text-lg text-purple-600">R$ 7,50</span>
                      
                        </div>
                        <button class="add-to-cart bg-purple-600 hover:bg-purple-700 text-white px-3 py-1 rounded-full text-sm transition duration-300" 
                                data-id="4" 
                                data-name="Smartphone Galaxy S21" 
                                data-price="3299.90" 
                                data-image="https://images.unsplash.com/photo-1546868871-7041f2a55e12?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=600&q=80">
                            <i class="fas fa-cart-plus"></i> Adicionar
                        </button>
                    </div>
                </div>
            </div>


    <!-- Shopping Cart Sidebar -->
    <div id="cart-sidebar" class="cart-slide cart-closed fixed inset-y-0 right-0 w-full sm:w-96 bg-white shadow-xl z-20">
        <div class="flex flex-col h-full">
            <!-- Cart Header -->
            <div class="flex justify-between items-center p-4 border-b">
                <h2 class="text-xl font-bold text-gray-800 flex items-center">
                    <i class="fas fa-shopping-cart mr-2 text-purple-600"></i>
                    Seu Carrinho
                </h2>
                <button id="close-cart" class="text-gray-500 hover:text-gray-700">
                    <i class="fas fa-times text-xl"></i>
                </button>
            </div>
            
            <!-- Cart Items -->
            <div class="cart-items flex-grow overflow-y-auto p-4">
                <div id="cart-empty" class="text-center py-12">
                    <i class="fas fa-shopping-cart text-4xl text-gray-300 mb-4"></i>
                    <h3 class="text-lg font-medium text-gray-500 mb-2">Seu carrinho está vazio</h3>
                    <p class="text-gray-400 text-sm">Adicione itens para começar a comprar</p>
                </div>
                <div id="cart-content" class="hidden">
                    <!-- Cart items will be added here dynamically -->
                </div>
            </div>
            
            <!-- Cart Summary -->
            <div id="cart-summary" class="hidden border-t p-4 bg-gray-50">
                <div class="flex justify-between mb-2">
                    <span class="text-gray-600">Subtotal:</span>
                    <span id="cart-subtotal" class="font-medium">R$ 0,00</span>
                </div>
                <div class="flex justify-between mb-4">
                    <span class="text-gray-600">Frete:</span>
                    <span id="cart-shipping" class="font-medium">Gratuito</span>
                </div>
                <div class="flex justify-between text-lg font-bold border-t pt-2">
                    <span>Total:</span>
                    <span id="cart-total" class="text-purple-600">R$ 0,00</span>
                </div>
                <button id="checkout-btn" class="w-full bg-purple-600 hover:bg-purple-700 text-white py-3 rounded-lg font-medium mt-4 transition duration-300">
                    Finalizar Compra
                </button>
            </div>
        </div>
    </div>

    <!-- Overlay -->
    <div id="overlay" class="fixed inset-0 bg-black bg-opacity-50 z-10 hidden"></div>

    <!-- Added to cart notification -->
    <div id="notification" class="fixed top-4 right-4 bg-green-500 text-white px-4 py-2 rounded-lg shadow-lg hidden fade-in z-30">
        <i class="fas fa-check-circle mr-2"></i>
        <span id="notification-message">Item adicionado ao carrinho!</span>
    </div>

    <script>
        // Document Ready
        document.addEventListener('DOMContentLoaded', function() {
            // Initialize cart
            let cart = JSON.parse(localStorage.getItem('cart')) || [];
            
            // DOM Elements
            const cartToggle = document.getElementById('cart-toggle');
            const closeCart = document.getElementById('close-cart');
            const cartSidebar = document.getElementById('cart-sidebar');
            const overlay = document.getElementById('overlay');
            const cartCount = document.getElementById('cart-count');
            const cartContent = document.getElementById('cart-content');
            const cartEmpty = document.getElementById('cart-empty');
            const cartSummary = document.getElementById('cart-summary');
            const cartSubtotal = document.getElementById('cart-subtotal');
            const cartTotal = document.getElementById('cart-total');
            const addToCartButtons = document.querySelectorAll('.add-to-cart');
            const notification = document.getElementById('notification');
            
            // Update cart count
            function updateCartCount() {
                const totalItems = cart.reduce((total, item) => total + item.quantity, 0);
                cartCount.textContent = totalItems;
                
                if (totalItems === 0) {
                    cartCount.classList.add('hidden');
                } else {
                    cartCount.classList.remove('hidden');
                }
            }
            
            // Update cart UI
            function updateCartUI() {
                if (cart.length === 0) {
                    cartContent.classList.add('hidden');
                    cartEmpty.classList.remove('hidden');
                    cartSummary.classList.add('hidden');
                    return;
                }
                
                cartContent.innerHTML = '';
                let subtotal = 0;
                
                cart.forEach((item, index) => {
                    const itemTotal = item.price * item.quantity;
                    subtotal += itemTotal;
                    
                    const cartItem = document.createElement('div');
                    cartItem.className = 'flex items-center py-3 border-b last:border-b-0 last:pb-0';
                    cartItem.innerHTML = `
                        <div class="flex-shrink-0 w-16 h-16 bg-gray-200 rounded-md overflow-hidden">
                            <img src="${item.image}" alt="${item.name}" class="w-full h-full object-cover">
                        </div>
                        <div class="ml-3 flex-grow">
                            <h3 class="text-sm font-medium text-gray-800">${item.name}</h3>
                            <div class="flex justify-between mt-1">
                                <span class="text-sm text-gray-500">R$ ${item.price.toFixed(2).replace('.', ',')}</span>
                                <div class="flex items-center">
                                    <button class="change-quantity px-2 text-gray-500 hover:text-purple-600" data-index="${index}" data-change="-1">
                                        <i class="fas fa-minus text-xs"></i>
                                    </button>
                                    <span class="quantity mx-2 text-sm">${item.quantity}</span>
                                    <button class="change-quantity px-2 text-gray-500 hover:text-purple-600" data-index="${index}" data-change="1">
                                        <i class="fas fa-plus text-xs"></i>
                                    </button>
                                </div>
                            </div>
                            <div class="mt-1 flex justify-between items-center">
                                <span class="text-xs text-gray-500">Total: R$ ${itemTotal.toFixed(2).replace('.', ',')}</span>
                                <button class="remove-item text-red-500 hover:text-red-700 text-xs" data-index="${index}">
                                    <i class="fas fa-trash-alt"></i> Remover
                                </button>
                            </div>
                        </div>
                    `;
                    cartContent.appendChild(cartItem);
                });
                
                // Update summary
                cartSubtotal.textContent = `R$ ${subtotal.toFixed(2).replace('.', ',')}`;
                cartTotal.textContent = `R$ ${subtotal.toFixed(2).replace('.', ',')}`;
                
                // Show cart content and summary
                cartContent.classList.remove('hidden');
                cartEmpty.classList.add('hidden');
                cartSummary.classList.remove('hidden');
            }
            
            // Save cart to localStorage
            function saveCart() {
                localStorage.setItem('cart', JSON.stringify(cart));
                updateCartCount();
                updateCartUI();
            }
            
            // Add to cart function
            function addToCart(product) {
                const existingItem = cart.find(item => item.id === product.id);
                
                if (existingItem) {
                    existingItem.quantity += 1;
                } else {
                    cart.push({
                        ...product,
                        quantity: 1
                    });
                }
                
                saveCart();
                
                // Show notification
                const notificationMessage = document.getElementById('notification-message');
                notificationMessage.textContent = `${product.name} adicionado ao carrinho!`;
                notification.classList.remove('hidden');
                
                setTimeout(() => {
                    notification.classList.add('hidden');
                }, 3000);
            }
            
            // Change quantity
            function handleQuantityChange(index, change) {
                cart[index].quantity += change;
                
                if (cart[index].quantity <= 0) {
                    cart.splice(index, 1);
                }
                
                saveCart();
            }
            
            // Remove item
            function removeItem(index) {
                cart.splice(index, 1);
                saveCart();
            }
            
            // Toggle cart
            function toggleCart() {
                cartSidebar.classList.toggle('cart-open');
                cartSidebar.classList.toggle('cart-closed');
                overlay.classList.toggle('hidden');
                
                // Prevent body scroll when cart is open
                if (cartSidebar.classList.contains('cart-open')) {
                    document.body.style.overflow = 'hidden';
                } else {
                    document.body.style.overflow = 'auto';
                }
            }
            
            // Event Listeners
            cartToggle.addEventListener('click', toggleCart);
            closeCart.addEventListener('click', toggleCart);
            overlay.addEventListener('click', toggleCart);
            
            // Add to cart buttons
            addToCartButtons.forEach(button => {
                button.addEventListener('click', function() {
                    const product = {
                        id: this.dataset.id,
                        name: this.dataset.name,
                        price: parseFloat(this.dataset.price),
                        image: this.dataset.image
                    };
                    addToCart(product);
                });
            });
            
            // Delegated events for dynamically added elements
            document.addEventListener('click', function(e) {
                // Quantity changes
                if (e.target.closest('.change-quantity')) {
                    const button = e.target.closest('.change-quantity');
                    const index = parseInt(button.dataset.index);
                    const change = parseInt(button.dataset.change);
                    handleQuantityChange(index, change);
                }
                
                // Remove item
                if (e.target.closest('.remove-item')) {
                    const button = e.target.closest('.remove-item');
                    const index = parseInt(button.dataset.index);
                    removeItem(index);
                }
            });
            
            // Checkout button
            document.getElementById('checkout-btn').addEventListener('click', function() {
                alert('Compra finalizada com sucesso! Obrigado por comprar conosco.');
                cart = [];
                saveCart();
                toggleCart();
            });
            
            // Initialize UI
            updateCartCount();
            updateCartUI();
        });
    </script>

    <footer class="bg-gray-800 text-white py-8">
        <div class="container mx-auto px-4">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <div>
                    <h3 class="text-lg font-bold mb-4 flex items-center">
                        <i class="fas fa-shopping-bag text-purple-400 mr-2"></i> LojaOnline
                    </h3>
                    <p class="text-gray-400">A melhor loja online para encontrar os produtos que você deseja com os melhores preços.</p>
                </div>
                <div>
                    <h3 class="text-lg font-bold mb-4">Links Rápidos</h3>
                    <ul class="space-y-2">
                        <li><a href="#" class="text-gray-400 hover:text-purple-400">Home</a></li>
                        <li><a href="#" class="text-gray-400 hover:text-purple-400">Produtos</a></li>
                        <li><a href="#" class="text-gray-400 hover:text-purple-400">Ofertas</a></li>
                        <li><a href="#" class="text-gray-400 hover:text-purple-400">Contato</a></li>
                    </ul>
                </div>
                <div>
                    <h3 class="text-lg font-bold mb-4">Contato</h3>
                    <ul class="space-y-2">
                        <li class="flex items-center text-gray-400">
                            <i class="fas fa-envelope mr-2 text-purple-400"></i> contato@lojaonline.com
                        </li>
                        <li class="flex items-center text-gray-400">
                            <i class="fas fa-phone mr-2 text-purple-400"></i> (11) 9999-9999
                        </li>
                        <li class="flex items-center text-gray-400">
                            <i class="fas fa-map-marker-alt mr-2 text-purple-400"></i> São Paulo, SP
                        </li>
                    </ul>
                </div>
            </div>
            <div class="border-t border-gray-700 mt-8 pt-8 text-center text-gray-400">
                <p>&copy; 2023 LojaOnline. Todos os direitos reservados.</p>
            </div>
        </div>
    </footer>
</body>
</html>
